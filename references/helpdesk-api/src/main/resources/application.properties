# spring.datasource.url=jdbc:postgresql://helpdesk.cvuygvkudxwb.us-east-2.rds.amazonaws.com:5432/postgres
# spring.datasource.username=interns
# spring.datasource.password=Synergy123
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.application.name=helpdesk
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
# spring.jpa.hibernate.ddl-auto=update

# spring.servlet.multipart.max-file-size=2MB
# spring.servlet.multipart.max-request-size=2MB

# server.port=8080
# security.basic.enable=false
# security.ignored=/**

# cloud.aws.creditials.profiles-name=nlpurcell
# cloud.aws.region.auto=false
# cloud.aws.region.static=us-east-2

# cloud.aws.rds.instances[0].db-instance-identifier=helpdesk
# cloud.aws.rds.instances[0].username=interns
# cloud.aws.rds.instances[0].password=Synergy123
# cloud.aws.rds.instances[0].databaseName=helpdesk

#server.port = 8083
#keycloak.realm = Helpdesk-test
#keycloak.auth-server-url = http://localhost:8080/auth
#keycloak.ssl-required = all #This should be `all` only in production and should be `external` otherwise (forces use of HTTPS)
#keycloak.resource = Helpdesk-test-app
#keycloak.use-resource-role-mappings = true
#keycloak.bearer-only = true
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=eodims.helpdesk@gmail.com
spring.mail.password=lqfbsqqxoegmwauh
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true